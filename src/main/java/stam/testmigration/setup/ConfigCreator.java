package stam.testmigration.setup;

import java.io.FileOutputStream;
import java.io.IOException;
import java.io.OutputStream;
import java.util.Properties;

public class ConfigCreator {
    public void createConfigFile() {
        /*
        Properties prop = new Properties();
        OutputStream output = null;
        try {
            output = new FileOutputStream("config.properties");

            prop.setProperty("sourcedir", "D:\\TestMigration\\dataset\\notes-dataset\\mariok#pinemup\\pinemup");
            prop.setProperty("targetdir", "D:\\TestMigration\\dataset\\notes-dataset\\Allanov#Note\\Note");
            prop.setProperty("jdkRootDir", "C:\\Program Files\\Java\\jdk1.8.0_261");
            prop.setProperty("gradlepath", "C:\\Gradle\\gradle-6.5\\bin");
            //Lavenshtein = 1, Word2Vec = 2, Lavenshtein+Word2Vec = 3
            prop.setProperty("simMethod", "3");
            //Suggested values: Lavenshtein = 0.5, Word2Vec = 0.5, Lavenshtein+Word2Vec = 0.4
            prop.setProperty("simThreshold", "0.4");

            prop.store(output, null);

            output.close();

        } catch (IOException io) {
            io.printStackTrace();
        }

         */
    }
}
